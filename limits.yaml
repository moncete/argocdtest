apiVersion: v1
kind: Deployment
metadata:
  name: grafana
spec:
  template:
    spec:
      containers:
      - name: grafana
        resources:
          limits:
            memory: 512Mi
          requests:
            memory: 256Mi