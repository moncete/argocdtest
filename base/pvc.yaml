apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
 name: "grafana"
spec:
 storageClassName: manual
 accessModes:
  - "ReadWriteOnce"
 resources:
  requests:
   storage: "1Gi"
 volumeName: pv0001
